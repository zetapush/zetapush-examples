{
  "name": "worker",
  "version": "0.1.0",
  "main": "worker/index.ts",
  "private": true,
  "scripts": {
    "deploy": "zeta push",
    "start": "zeta run",
    "troubleshoot": "zeta troubleshoot",
    "serve": "(cd src && npm run start)",
    "build": "(cd src && npm run build)",
    "test": "(cd src && npm run test)",
    "eject": "(cd src && npm run eject)"
  },
  "dependencies": {
    "@zetapush/cli": "^0.37.7",
    "@zetapush/core": "^0.37.7",
    "@zetapush/platform-legacy": "^0.37.7"
  }
}
